<h3><%= web.name %> The Web</h3>

<p>Size: <%= web.size %></p>

<% if web.spider.present? %>
	<p>I am <%= web.spider.name %> The Spider's Web!</p>
<% else %>

<% end %>

<small>
<%= link_to "[delete]", web_path(web), method: :delete %>
<%= link_to "[edit]", edit_web_path(web) %>
</small>
